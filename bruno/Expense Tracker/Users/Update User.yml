info:
  name: Update User
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/users/:id"
  params:
    - name: id
      value: 6a02a92f8006499d7be0eb8e
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Zach (updated)"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
