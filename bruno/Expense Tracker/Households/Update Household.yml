info:
  name: Update Household
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/households/:id"
  params:
    - name: id
      value: 6a02a111ef724c7d5fd3629d
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Sidera (updated)"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
