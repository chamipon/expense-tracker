info:
  name: Add Household
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/households"
  body:
    type: json
    data: |-
      {
        "name": "Sidera"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
