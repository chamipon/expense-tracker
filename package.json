{
	"scripts": {
		"env": "tsx scripts/sync-env.ts",
        "dev": "pnpm -r --parallel dev",
        "build": "pnpm -r build"
	},
	"dependencies": {
		"pnpm": "^11.0.9"
	},
	"devDependencies": {
		"dotenv": "^17.4.2",
		"typescript": "^5"
	}
}
